global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'tadpole-os'
    scrape_interval: 5s  # High-frequency scraping for real-time engine metrics
    metrics_path: '/metrics'
    static_configs:
      - targets: ['tadpole:8000']
